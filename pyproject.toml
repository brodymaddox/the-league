[project]
name = "the-league"
version = "0.1.0"
description = "Multi-agent RL competition league"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
